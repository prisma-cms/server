

type File {
  id: ID! @unique
  path: String!
  filename: String!
  mimetype: String!
  encoding: String!
  createdby: User! @relation(name: "UserFiles")

  # Используется для фиксации уникальных файлов
  hash: String
}
